import React from "react";
import ReactDOM from "react-dom";
import App from "./Search_filter";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
